struct BoolType {}
struct StrType {}

struct IntType {
    name Str
    signed Bool
    bits Int
}

union Type = BoolType | StrType | IntType

fun type_of<T>() Type {
    panic("comp::type_of must only be used as a macro argument")
    StrType()
}
