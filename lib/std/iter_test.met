use std::test
use std::iter
use std::list

fun main() void {
    let @local = Arena()
    let suite = test::Suite.new("iter", @local)
    suite.test("count", fun() !void {
        mut it = "hello".iter()
        try test::eq(5, iter::count(&mut it))
    })
    suite.test("map", fun() !void {
        mut it = "hello".iter()
        mut sut = iter::map(&mut it, fun(a Rune) U8 { a.to_u32().to_u8_wrapping() })
        try test::eq(Option(U8(104)), (&mut sut).next())
        try test::eq(Option(U8(101)), (&mut sut).next())
        try test::eq(Option(U8(108)), (&mut sut).next())
        try test::eq(Option(U8(108)), (&mut sut).next())
        try test::eq(Option(U8(111)), (&mut sut).next())
        try test::eq(Option<U8>(None()), (&mut sut).next())
    })
    suite.test("filter", fun() !void {
        mut it = "hello".iter()
        mut sut = iter::filter(&mut it, fun(a Rune) Bool { a == 'l' })
        try test::eq(Option('l'), (&mut sut).next())
        try test::eq(Option('l'), (&mut sut).next())
        try test::eq(Option<Rune>(None()), (&mut sut).next())
    })
    suite.test("all", fun() !void {
        mut it = "aaaaa".iter()
        try test::eq(true, iter::all(&mut it, fun(a Rune) Bool { a == 'a' }))
        it = "aaaab".iter()
        try test::eq(false, iter::all(&mut it, fun(a Rune) Bool { a == 'a' }))
    })
    suite.test("any", fun() !void {
        mut it = "aaaba".iter()
        try test::eq(true, iter::any(&mut it, fun(a Rune) Bool { a == 'b' }))
        it = "aaaaa".iter()
        try test::eq(false, iter::any(&mut it, fun(a Rune) Bool { a == 'b' }))
    })
    suite.test("chain", fun() !void {
        let @local = Arena()
        mut it1 = "aa".iter()
        let l = list::List.new<Rune>(32, @local)
        l.push('b')
        l.push('b')
        mut it2 = l.iter()
        mut sut = iter::chain(&mut it1, &mut it2)
        try test::eq(Option('a'), (&mut sut).next())
        try test::eq(Option('a'), (&mut sut).next())
        try test::eq(Option('b'), (&mut sut).next())
        try test::eq(Option('b'), (&mut sut).next())
        try test::eq(Option<Rune>(None()), (&mut sut).next())
    })
    suite.test("fold", fun() !void {
        let @local = Arena()
        let l = list::List.new<U8>(32, @local)
        l.push(1)
        l.push(2)
        l.push(3)
        mut it = l.iter()
        try test::eq(16, iter::fold(&mut it, 10, fun(sum Int, v U8) Int { sum + v.to_int() }))
    })
    suite.test("peekable", fun() !void {
        mut it = "hello".iter()
        mut sut = iter::peekable(&mut it)
        try test::eq(Option('h'), (&mut sut).peek())
        try test::eq(Option('h'), (&mut sut).peek())
        try test::eq(Option('h'), (&mut sut).next())
        try test::eq(Option('e'), (&mut sut).peek())
        try test::eq(Option('e'), (&mut sut).next())
        try test::eq(Option('l'), (&mut sut).next())
        try test::eq(Option('l'), (&mut sut).next())
        try test::eq(Option('o'), (&mut sut).peek())
        try test::eq(Option('o'), (&mut sut).next())
        try test::eq(Option<Rune>(None()), (&mut sut).peek())
        try test::eq(Option<Rune>(None()), (&mut sut).next())
    })
    suite.test("take", fun() !void {
        mut it = "hello".iter()
        mut sut = iter::take(&mut it, 2)
        try test::eq(Option('h'), (&mut sut).next())
        try test::eq(Option('e'), (&mut sut).next())
        try test::eq(Option<Rune>(None()), (&mut sut).next())
        try test::eq(Option('l'), (&mut it).next())

        it = "hi".iter()
        sut = iter::take(&mut it, 10)
        try test::eq(Option('h'), (&mut sut).next())
        try test::eq(Option('i'), (&mut sut).next())
        try test::eq(Option<Rune>(None()), (&mut sut).next())
    })
    suite.test("take_while", fun() !void {
        mut it = "hello".iter()
        mut peekable_it = iter::peekable(&mut it)
        mut sut = iter::take_while(&mut peekable_it, fun(r Rune) Bool { r != 'l'})
        try test::eq(Option('h'), (&mut sut).next())
        try test::eq(Option('e'), (&mut sut).next())
        try test::eq(Option<Rune>(None()), (&mut sut).next())
        try test::eq(Option('l'), (&mut it).next())

        -- list.iter() is already peekable
        let @local = Arena()
        mut l = list::List.new<Int>(32, @local)
        l.push(1)
        l.push(2)
        l.push(3)
        mut list_iter = l.iter()
        mut list_sut = iter::take_while(&mut list_iter, fun(i Int) Bool { i < 3 })
        try test::eq(Option(1), (&mut list_sut).next())
        try test::eq(Option(2), (&mut list_sut).next())
        try test::eq(Option<Int>(None()), (&mut list_sut).next())
        try test::eq(Option(3), (&mut list_iter).next())
    })
    suite.test("collect", fun() !void {
        let @local = Arena()
        mut it = "hello".iter()
        mut l = list::List.new<Rune>(32, @local)
        iter::collect(&mut it, l)
        try test::eq(5, l.len)
    })
    suite.test("find", fun() !void {
        mut it = "hello".iter()
        try test::eq(Option('e'), iter::find(&mut it, fun(a Rune) Bool { a == 'e' }))
        try test::eq(Option<Rune>(None()), iter::find(&mut it, fun(a Rune) Bool { a == '!' }))
    })
    suite.test("combine multiple", fun() !void {
        mut it = "hello".iter()
        mut map = iter::map(&mut it, fun(a Rune) U8 { a.to_u32().to_u8_wrapping() })
        mut filter = iter::filter(&mut map, fun(a U8) Bool { a == 108 })
        try test::eq(2, iter::count(&mut filter))
    })
    _ = suite.run()
}
