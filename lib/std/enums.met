--- Return a slice with all variants of the given enum. ---
pub fun variants<T Enum>() []T {
    panic("implemented by the compiler")
}

--- Try to map the given integer tag to an enum variant.

    Note: Open enums don't have stable tags, i.e. they change from build to build. ---
pub fun from_tag<T Enum>(t T.Item) ?T {
    panic("implemented by the compiler")
}
