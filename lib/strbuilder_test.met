use std::test
use std::list

fun main() void {
    let @local = Arena()

    let suite = test::Suite.new("StrBuilder", @local)
    suite.test("u64", fun() !void {
        let @local = Arena()
        let sut = StrBuilder.new(32, @local)
        sut.u64(13370)
        sut.str(" ")
        sut.u64(13370, 2)
        sut.str(" ")
        sut.u64(13370, 8)
        sut.str(" ")
        sut.u64(13370, 10)
        sut.str(" ")
        sut.u64(13370, 16)
        sut.str(" ")
        sut.u64(13370, 16, true)
        try test::eq("13370 11010000111010 32072 13370 343a 343A", sut.to_str())
    })
    suite.test("int", fun() !void {
        let @local = Arena()
        let sut = StrBuilder.new(32, @local)
        sut.int(13370)
        sut.str(" ")
        sut.int(0 - 13370)
        sut.str(" ")
        sut.int(13370, 2)
        sut.str(" ")
        sut.int(13370, 8)
        sut.str(" ")
        sut.int(13370, 10)
        sut.str(" ")
        sut.int(13370, 16)
        sut.str(" ")
        sut.int(13370, 16, true)
        try test::eq("13370 -13370 11010000111010 32072 13370 343a 343A", sut.to_str())
    })
    suite.test("slice", fun() !void {
        let @local = Arena()
        let sut = StrBuilder.new(32, @local)
        let s = ["hello", "there", "again"][..]

        sut.slice(s, " ")
        try test::eq("hello there again", sut.to_str())

        sut.reset()
        sut.slice(s, ", ", 13)
        try test::eq("hello, the...", sut.to_str())

        sut.reset()
        sut.slice(s, ", ", 3)
        try test::eq("...", sut.to_str())

        -- Make sure we don't truncate the output in the middle of a rune.
        let u = ["hi", "😀😀", "ha"][..]
        sut.reset()
        sut.slice(u, " ", 13)
        try test::eq("hi 😀...", sut.to_str())
    })
    suite.test("join", fun() !void {
        let @local = Arena()
        let sut = StrBuilder.new(32, @local)
        let l = list::List.new<Int>(@local)
        l.push(1)
        l.push(2)
        l.push(3)
        mut it = l.iter()
        sut.join(&mut it, ", ")
        try test::eq("1, 2, 3", sut.to_str())
    })
    _ = suite.run()
}
