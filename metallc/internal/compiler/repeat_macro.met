fun apply(n Int, sb &mut StrBuilder, @a Arena) void {
    sb.str("fun repeat() void {")
    sb.nl()
    for i in 0..n {
        sb.str("    DebugIntern.print_str(")
        sb.rune('"')
        sb.str("hello ")
        sb.rune('"')
        sb.str(")")
        sb.nl()
    }
    sb.str("}")
}
