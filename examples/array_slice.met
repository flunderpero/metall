use std::io

fun main() void {
    let arr = [0, 1, 2, 3, 4]
    for i in 0..arr.len {
        io::println(arr[i])
    }

    let @a = Arena()
    let slice = @a.slice_uninit<Int>(5)
    for i in 0..slice.len {
        io::println(slice[i])
    }
}
